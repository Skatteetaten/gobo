query getApplications($affiliations: [String!]) {
  applications(affiliations: $affiliations) {
    totalCount
      edges {
        node {
          name
          applicationInstances {
            affiliation {
              name
            }
            namespace {
              name
            }
            details {
              buildTime
            }
          }
        }
      }
  }
}