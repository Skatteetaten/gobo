query getCname($affiliation: String!) {
  cname(affiliations: [$affiliation]) {
    azure {
      canonicalName
      clusterId
      ttlInSeconds
    }
    onPrem {
      status
      clusterId
      appName
    }
  }
}