query getCname($affiliation: String!) {
  cname(affiliations: [$affiliation]) {
    azure {
      canonicalName
      clusterId
      ttlInSeconds
      namespace
    }
    onPrem {
      status
      clusterId
      appName
    }
  }
}