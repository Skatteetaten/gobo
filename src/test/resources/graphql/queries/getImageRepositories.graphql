query getImageRepositories($repositories: [String!]!) {
    imageRepositories(repositories: $repositories) {
        repository
        guiUrl
        tags {
            totalCount
            edges {
                node {
                    name
                    image {
                        buildTime
                        imageReference
                    }
                }
            }
        }
    }
}