query getRoute($namespace: String!, $name: String!) {
    route(namespace: $namespace, name: $name) {
       progressions {
         id
         payload
         objectname
         namespace
         type
         operation
         status
         updated
         errorMessage
       }
    }
}
