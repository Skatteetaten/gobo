query getAffiliation($name: String!) {
  affiliations(name: $name) {
    edges {
      node {
        name
        auroraConfig {
          name
          files {
            name
          }
        }
      }
    }
  }
}
