query getAffiliation($name: String!) {
  affiliations(names: [$name]) {
    edges {
      node {
        name
        auroraConfig {
          name
          files {
            name
          }
        }
      }
    }
  }
}
