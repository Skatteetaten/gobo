query getApplicationDeployment($id: String!) {
  applicationDeployment(id: $id) {
    environment
    name
    toxiProxy{
        name
        listen
        upstream
        enabled
        toxics {
           name
           type
           attributes {
              key
              value
           }
        }
    }
  }
}