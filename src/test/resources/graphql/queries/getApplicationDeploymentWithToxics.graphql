query getApplicationDeployment($id: String!) {
  applicationDeployment(id: $id) {
    environment
    name
    toxics {
       name
       type
       attributes {
          key
          value
       }
    }

  }
}