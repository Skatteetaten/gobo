query getEnvironments($name: String!) {
  environments(names: [$name]) {
    name
    affiliations {
      name
      applications {
        name
        status {
          state
          message
          details
        }
      }
    }
  }
}