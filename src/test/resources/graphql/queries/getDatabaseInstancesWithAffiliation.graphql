query getDatabaseInstances($affiliation: String!) {
    databaseInstances(affiliation: $affiliation) {
        engine
        instanceName
        host
        port
        createSchemaAllowed
        affiliation {
            name
        }
    }
}