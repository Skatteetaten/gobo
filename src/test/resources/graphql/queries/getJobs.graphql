query getRoutes($namespace: String!, $name: String!) {
    routes(namespace: $namespace, name: $name) {
       progressions {
         id
         payload
         objectname
         namespace
         type
         operation
         status
         updated
         errorMessage
       }
    }
}
