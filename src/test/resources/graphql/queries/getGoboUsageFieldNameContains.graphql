query getApplicationDeployment($nameContains: String!) {
    gobo {
        usage {
            usedFields(nameContains: $nameContains) {
                name
                count
            }
        }
    }
}