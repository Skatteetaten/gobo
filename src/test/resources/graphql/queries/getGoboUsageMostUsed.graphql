query getMostUsedFields($mostUsedOnly: Boolean!) {
    gobo {
        startTime
        usage {
            usedFields(mostUsedOnly: $mostUsedOnly) {
                name
            }
        }
    }
}