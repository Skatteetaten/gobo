query getUserSettings($affiliations: [String!]) {
    userSettings {
        applicationDeploymentFilters(affiliations: $affiliations) {
            name
            affiliation
            applications
            environments
        }
    }
}