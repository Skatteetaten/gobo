mutation deployEnvironment($input: DeploymentEnvironmentInput!) {
  deployEnvironment(input: $input) {
    deploymentRef {
      cluster
      affiliation
      environment
      application
    }
    deployId
    timestamp
    message
  }
}