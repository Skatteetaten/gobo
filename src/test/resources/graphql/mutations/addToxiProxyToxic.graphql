mutation addToxiProxyToxic($input: AddToxiProxyToxicInput!) {
  addToxiProxyToxic(input: $input) {
    podName
    toxics {
        type
        toxicName
        toxicAttributes {
            attrName //latency, jitter
            attrValue
        }
    }
}