package no.skatteetaten.aurora.gobo.integration.imageregistry

class OverrideRegistryImageRegistryUrlBuilderTest {
/*

    @Test
    fun `Create api url`() {
        val urlBuilder = ImageRegistryUrlBuilder("http://registry-url")
        val apiUrl = urlBuilder.createApiUrl("", ImageRepoDto("", "namespace", "name"))
        assertThat(apiUrl).isEqualTo("http://registry-url/v2/namespace/name")
    }
*/
}