scalar DateTime
scalar URL

type Query {
  affiliations: AffiliationsConnection

  applications(affiliations: [String!]!, applications: [String!]): ApplicationsConnection

  imageRepositories(repositories: [String!]): [ImageRepository!]

  currentUser: User
}

type Mutation {
  updateApplicationDeploymentVersion(input: ApplicationDeploymentVersionInput): ApplicationDeployment
}

type PageInfo {
  startCursor: String
  endCursor: String
  hasPreviousPage: Boolean
  hasNextPage: Boolean
}
