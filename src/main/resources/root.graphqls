type Query {
  affiliations(first: Int, after: String): AffiliationsConnection

  applications(affiliations: [String!]): ApplicationsConnection
}

type PageInfo {
  startCursor: String
  endCursor: String
  hasNextPage: Boolean
}
