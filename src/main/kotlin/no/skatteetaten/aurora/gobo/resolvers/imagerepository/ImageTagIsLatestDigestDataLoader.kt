package no.skatteetaten.aurora.gobo.resolvers.imagerepository

/*
@Component
class ImageTagDtoDataLoader(private val imageRegistryServiceBlocking: ImageRegistryServiceBlocking) :
    KeyDataLoader<ImageTagDigestDTO, ImageTagDto> {
    override suspend fun getByKey(key: ImageTagDigestDTO, context: GoboGraphQLContext):  = null


        /*if (key.imageTag.imageRepository.registryUrl == null) {
            null
        } else {
            val imageRepoDto = key.imageTag.imageRepository.toImageRepo()
            imageRegistryServiceBlocking.findImageTagDto(
                imageRepoDto,
                key.imageTag.name,
                context.token!!
            )
        }
        */
}ApplicationDeploymentQueryResolverTest
 */
