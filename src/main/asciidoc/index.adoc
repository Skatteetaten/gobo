# Aurora API
:!revnumber:
:source-highlighter: highlight.js
Table of contents:


. link:toxiproxy.html[Toxi-proxy and toxics queries and mutations]

When using the queries and mutations, you need to specify the Authorization tag :
[source,json]
{
  "Authorization": "Bearer <bearer-tag>"
}
(The bearer-tag is obtained through doing an oc login on the desired cluster, plus command: oc whoami -t)

[source,graphql]
include::queries/getAffiliation.graphql[]

[source,json]
{
  "test": "test"
}